# As chaves de API ficam em .secrets/.env (pasta ignorada pelo Git).
#
# Setup local:
#   1. Copie o modelo:  cp .secrets/.env.example .secrets/.env
#   2. Edite .secrets/.env com sua chave e ID da planilha
#   3. npm run dev
#
# Veja .secrets/.env.example para a lista completa de variáveis.
